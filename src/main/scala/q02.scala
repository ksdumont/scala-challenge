object q02 {
  def main(args: Array[String]): Unit = {
    // get length of args
    val length: Int = args.length

    // check to make sure there are at least two numbers
    if (length < 2) {
      println("Please provide at least two cities in the arguments.")
      return
    }

  }
}
